I only updated the charge.php file to be be this:
make sure you set the key to the PRIVATE KEY, and then be sure to REDEPLOY to Heroku.

<?php
  require_once('vendor/autoload.php');
  
  \Stripe\Stripe::setApiKey("sk_test_private_api_key");
  $token = $_POST['stripeToken'];
  $amount = $_POST['amount'];
  $currency = $_POST['currency'];
  $description = $_POST['description'];
  try {
   $charge = \Stripe\Charge::create(array(
   "amount" => $amount*100, // Convert amount in cents to dollar
   "currency" => $currency,
   "source" => $token,
   "description" => $description)
   );
  // Check that it was paid:
   if ($charge->paid == true) {
   $response = array("status"=>"Success","message"=>"Payment_been_charged!!");
   } else { // Charge was not paid!
   $response = array( 'status'=> 'Failure', 'message'=>'Your_payment_could NOT be processed because the payment system rejected the transaction. You can try again or use another card.' );
   }
   header('Content-Type: application/json');
   echo json_encode($response);
  } catch(\Stripe\Error\Card $e) {
   // The card has been declined
  header('Content-Type: application/json');
   echo json_encode($response);
  }
?>
